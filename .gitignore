# dependencies
node_modules/

# opencode runtime/cache
.opencode/auth.json

# MOA local data (long-term memory, budget) lives in ~/.moa, but ignore any
# stray local copies just in case
.moa/

# internal dev notes (not for public repo)
PROGRESS.md

# env
.env
.env.*
!.env.example
!.env.docker.example

# OS noise
Thumbs.db
.DS_Store
